package util;

import aws.DatabaseToS3Uploader;

public class Login {
    public static void main(String[] args) {
     // DatabaseToS3Uploader.uploadS3();
        Utility util = new Utility();
        util.validaLogin();
    }
}
